docker network create webapp
docker run -d -e CURRENT_ENVIRONMENT=development --name back-end-service --network webapp backend:1.0
docker run -d -p 8080:80 --name front-end-service --network webapp frontend:1.0
